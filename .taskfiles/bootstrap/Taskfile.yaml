---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:
  main:
    cmds:
      - task: :terraform:apply
      - task: :terraform:output
      - task: :flux:bootstrap
