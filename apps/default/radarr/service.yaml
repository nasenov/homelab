apiVersion: v1
kind: Service
metadata:
  name: radarr
  namespace: default
  labels:
    app: radarr
spec:
  ports:
  - name: web
    port: 80
    protocol: TCP
    targetPort: 7878
  selector:
    app: radarr
