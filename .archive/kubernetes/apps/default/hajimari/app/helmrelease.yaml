---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: hajimari
  namespace: default
spec:
  interval: 10m0s
  chart:
    spec:
      chart: hajimari
      version: 2.0.2
      sourceRef:
        kind: HelmRepository
        name: hajimari
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    env:
      TZ: Europe/Sofia

    hajimari:
      defaultEnable: true    
      namespaceSelector:
        matchNames:
          - default
          - longhorn-system
          - monitoring
      title: Apps
      darkTheme: lime
      showGreeting: false
      showAppGroups: true
      showAppUrls: false
      showAppStatus: false
      showBookmarkGroups: false
      showGlobalBookmarks: false
      alwaysTargetBlank: true
      customApps:
        - group: Infrastructure
          apps:
            - name: Proxmox
              url: 'https://proxmox.nasenov.dev:8006'
              icon: 'mdi:server'
            - name: Truenas
              url: 'https://truenas.nasenov.dev'
              icon: 'mdi:nas'

    podAnnotations:
      configmap.reloader.stakater.com/reload: "hajimari-settings"

    ingress:
      main:
        enabled: true
        annotations:
          hajimari.io/enable: "false"
        ingressClassName: nginx
        hosts:
          - host: apps.nasenov.dev
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - apps.nasenov.dev
    
    persistence:
      data:
        enabled: true
        type: emptyDir
