---
extends: default

ignore:
  - "*.sops.*"

rules:
  braces:
    max-spaces-inside: 1

  comments:
    min-spaces-from-content: 1

  line-length: disable

  truthy:
    allowed-values: ["true", "false", "on"]
